server:
  port: 8082
  
spring:
  application:
    name: activemq-jms-listener

  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false

